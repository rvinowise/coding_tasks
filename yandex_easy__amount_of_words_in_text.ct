// yandex task 1. Количество слов в тексте
fun main() {
    var line: String?
    var words: Set<String> = setOf();
    while (readlnOrNull().also {line = it} != null) {
        words = words union line!!.split("\\s+".toRegex()).toSet()
    }
    var words_amount = words.size
    //println("words: $words")
    println(words_amount)
}
